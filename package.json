{
  "name": "lambda-forms",
  "description": "The best way to handle forms on static websites.",
  "version": "1.0.0",
  "author": "jasmo2",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "yarn install && cd ./backend && yarn build && cd ../frontend/ && yarn build"
  }
}
